package ${package}.mapper;

import ${package}.entity.${className};
import com.isoftstone.ocp.common.core.Mapper;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Mapper extends Mapper<${className}> {
}
